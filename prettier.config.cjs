/**
 * This is a Prettier configuration file.
 * It uses the `prettier-plugin-tailwindcss` plugin to format Tailwind CSS classes.
 */
module.exports = {
  plugins: [require.resolve("prettier-plugin-tailwindcss")], // Load `prettier-plugin-tailwindcss` to format Tailwind CSS classes
};
